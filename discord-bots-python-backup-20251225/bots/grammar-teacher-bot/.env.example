# Discord Bot Configuration
DISCORD_BOT_TOKEN=your_discord_bot_token_here

# OpenAI Configuration (Required for AI-powered grammar checking)
OPENAI_API_KEY=your_openai_api_key_here

# OpenAI Budget Limit (USD per month)
# Bot will AUTOMATICALLY SUSPEND if this limit is reached to prevent overspending
# Budget resets on the 1st of each month
# Default: $10.00 (enough for ~30,000 grammar checks with gpt-4o-mini)
OPENAI_MAX_BUDGET=10.0

# How to get your OpenAI API key:
# 1. Go to https://platform.openai.com/api-keys
# 2. Sign in or create an account
# 3. Click "Create new secret key"
# 4. Copy the key and paste it above (starts with sk-)
# 5. Rename this file to .env

# Note: AI grammar checking uses GPT-4o-mini which is very affordable
# Typical cost: ~$0.0003 per grammar check
# At 1,000 checks/day: ~$2.70/month
# First $5 of usage is free for new accounts

# Budget Safety:
# - Bot tracks spending in real-time
# - Automatically suspends at budget limit
# - Resets monthly (1st of each month)
# - Use /budget command to check current usage
# - Adjust OPENAI_MAX_BUDGET to increase/decrease limit
